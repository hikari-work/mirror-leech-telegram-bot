# Development-only dependencies for the test suite.
pytest>=7.4
pytest-asyncio>=0.23
httpx>=0.27

# Type checking. Config lives in pyproject.toml, the debt ledger in
# pyrefly-baseline.json.
pyrefly>=1.2
