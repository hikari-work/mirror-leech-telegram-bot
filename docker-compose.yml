services:
  app:
    build: .
    command: bash start.sh
    restart: on-failure
    # One download task holds a socket per segment plus an open handle per
    # destination file, and a bulk of a few hundred files gets close to the
    # default soft limit of 1024. `start.sh` raises the limit for the running
    # process too, and that is the one that takes effect today: a change here
    # only lands when the container is recreated, and /app is not a volume, so
    # recreating it would take config.py, the session and the downloads with it.
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    network_mode: "host"
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    environment:
      # Encrypts private files (cookies.txt, .netrc, config.py) before they
      # are stored as blobs in PostgreSQL. Set it in a
      # .env file next to this compose file; never in config.py, whose
      # contents are themselves persisted to the database.
      #
      # Compose refuses to start if this is unset: an empty value would make
      # the bot fall back to storing those files as plaintext.
      #
      # Changing or losing the key makes previously stored blobs unreadable.
      # DATABASE_URL itself lives in config.py (this compose file predates the
      # PostgreSQL backend and sets no database variables).
      DB_ENCRYPTION_KEY: ${DB_ENCRYPTION_KEY:?set DB_ENCRYPTION_KEY in .env (any passphrase) before starting}
